<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start App"
        android:id="@+id/but_startapp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:onClick="showAuthentication"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="How to"
        android:id="@+id/but_howto"
        android:layout_marginTop="60dp"
        android:layout_below="@+id/but_startapp"
        android:layout_toLeftOf="@+id/but_startapp"
        android:layout_toStartOf="@+id/but_startapp"
        android:onClick="showHowto"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="About"
        android:id="@+id/but_about"
        android:layout_alignTop="@+id/but_howto"
        android:layout_toRightOf="@+id/but_startapp"
        android:layout_toEndOf="@+id/but_startapp"
        android:onClick="showAbout"/>
</RelativeLayout>
